# Item Service (FastAPI + PostgreSQL)

## 🚀 Инструкция по запуску

Для запуска проекта вам понадобится **Docker** и **Docker Compose**.

1. **Соберите и запустите контейнеры:**
   ```bash
   docker-compose up -d --build

2. **Перейдите по адресу: [http://localhost:8000/doc](http://localhost:8000/doc) и сделайте запросы:**

   *   **POST** `/api/items` — создать item;
   *   **GET** `/api/items` — получить список item-ов;
   *   **DELETE** `/api/items/{id}` — удалить конкретный item.
